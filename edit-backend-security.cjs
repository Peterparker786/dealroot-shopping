const fs = require("fs");
const p = "C:/Users/ST/Desktop/DEALROOT/dealroot-backend/src/server.js";
const src = fs.readFileSync(p, "utf8");

const edits = [];

// 1) Parameterize sendOTP so security flows can send their own subject/heading.
edits.push({
  from:
    "async function sendOTP(email, otp) {\n  await transporter.sendMail({\n    from: `\"Dealroot\" <${process.env.EMAIL_USER}>`,\n    to: email,\n    subject: \"Dealroot Password Reset OTP\",\n    html: `\n      <div style=\"font-family:Arial;padding:30px\">\n        <h2>Reset your password</h2>\n",
  to:
    "async function sendOTP(\n  email,\n  otp,\n  subject = \"Dealroot Password Reset OTP\",\n  heading = \"Reset your password\"\n) {\n  await transporter.sendMail({\n    from: `\"Dealroot\" <${process.env.EMAIL_USER}>`,\n    to: email,\n    subject,\n    html: `\n      <div style=\"font-family:Arial;padding:30px\">\n        <h2>${heading}</h2>\n",
});

// 2) Add pendingEmail / pendingPhone to userSchema (reuse resetOTP + otpExpiry).
edits.push({
  from:
    "otpExpiry: {\n  type: Date,\n  default: null,\n},\n    phone: { type: String, default: \"\", trim: true },",
  to:
    "otpExpiry: {\n  type: Date,\n  default: null,\n},\n    pendingEmail: { type: String, default: \"\", trim: true, lowercase: true },\n    pendingPhone: { type: String, default: \"\", trim: true },\n    phone: { type: String, default: \"\", trim: true },",
});

// 3) Insert the Login & Security endpoints before GET /api/auth/me.
edits.push({
  from:
    "\n\napp.get(\"/api/auth/me\", requireUser, async (req, res) => {\n  const user = await User.findById(req.user.userId);",
  to:
    "\n\n// ---- Login & Security: change email / phone / password (OTP to old contact) ----\n\napp.post(\"/api/auth/change-email-otp\", requireUser, async (req, res) => {\n  try {\n    const newEmail = String(req.body?.newEmail || \"\")\n      .trim()\n      .toLowerCase();\n\n    if (!/^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(newEmail)) {\n      return res\n        .status(400)\n        .json({ success: false, message: \"Please enter a valid email address\" });\n    }\n\n    const user = await User.findById(req.user.userId);\n    if (!user) {\n      return res.status(404).json({ success: false, message: \"Account not found\" });\n    }\n\n    if (newEmail === user.email) {\n      return res\n        .status(400)\n        .json({ success: false, message: \"This is already your email address\" });\n    }\n\n    const taken = await User.findOne({ email: newEmail });\n    if (taken) {\n      return res.status(409).json({\n        success: false,\n        message: \"That email is already linked to another account\",\n      });\n    }\n\n    const otp = Math.floor(100000 + Math.random() * 900000).toString();\n    user.resetOTP = otp;\n    user.otpExpiry = Date.now() + 10 * 60 * 1000;\n    user.pendingEmail = newEmail;\n    await user.save();\n\n    await sendOTP(\n      user.email,\n      otp,\n      \"DEALROOT — Verify your email change\",\n      \"Verify your email change\"\n    );\n\n    res.json({ success: true, message: \"OTP sent to your current email\" });\n  } catch (err) {\n    console.log(err);\n    res.status(500).json({ success: false, message: \"Could not send OTP\" });\n  }\n});\n\napp.post(\"/api/auth/verify-change-email\", requireUser, async (req, res) => {\n  try {\n    const otp = String(req.body?.otp || \"\").trim();\n    const user = await User.findById(req.user.userId);\n\n    if (!user) {\n      return res.status(404).json({ success: false, message: \"Account not found\" });\n    }\n    if (!user.pendingEmail) {\n      return res\n        .status(400)\n        .json({ success: false, message: \"No email change requested\" });\n    }\n    if (user.resetOTP !== otp || !user.otpExpiry || user.otpExpiry < Date.now()) {\n      return res\n        .status(400)\n        .json({ success: false, message: \"Invalid or expired OTP\" });\n    }\n\n    const taken = await User.findOne({\n      email: user.pendingEmail,\n      _id: { $ne: user._id },\n    });\n    if (taken) {\n      return res.status(409).json({\n        success: false,\n        message: \"That email is already linked to another account\",\n      });\n    }\n\n    user.email = user.pendingEmail;\n    user.pendingEmail = \"\";\n    user.resetOTP = \"\";\n    user.otpExpiry = null;\n    await user.save();\n\n    res.json({\n      success: true,\n      message: \"Email updated successfully\",\n      user: publicUser(user),\n    });\n  } catch (err) {\n    console.log(err);\n    res.status(500).json({ success: false, message: \"Could not update email\" });\n  }\n});\n\napp.post(\"/api/auth/change-phone-otp\", requireUser, async (req, res) => {\n  try {\n    const newPhone = String(req.body?.newPhone || \"\").replace(/\\D/g, \"\");\n\n    if (newPhone.length !== 10) {\n      return res\n        .status(400)\n        .json({ success: false, message: \"Please enter a valid 10-digit mobile number\" });\n    }\n\n    const user = await User.findById(req.user.userId);\n    if (!user) {\n      return res.status(404).json({ success: false, message: \"Account not found\" });\n    }\n\n    if (newPhone === user.phone) {\n      return res\n        .status(400)\n        .json({ success: false, message: \"This is already your mobile number\" });\n    }\n\n    const otp = Math.floor(100000 + Math.random() * 900000).toString();\n    user.resetOTP = otp;\n    user.otpExpiry = Date.now() + 10 * 60 * 1000;\n    user.pendingPhone = newPhone;\n    await user.save();\n\n    // No SMS gateway is configured, so the OTP goes to the verified email\n    // address on the account instead.\n    await sendOTP(\n      user.email,\n      otp,\n      \"DEALROOT — Verify your mobile number change\",\n      \"Verify your mobile number change\"\n    );\n\n    res.json({ success: true, message: `OTP sent to ${user.email}` });\n  } catch (err) {\n    console.log(err);\n    res.status(500).json({ success: false, message: \"Could not send OTP\" });\n  }\n});\n\napp.post(\"/api/auth/verify-change-phone\", requireUser, async (req, res) => {\n  try {\n    const otp = String(req.body?.otp || \"\").trim();\n    const user = await User.findById(req.user.userId);\n\n    if (!user) {\n      return res.status(404).json({ success: false, message: \"Account not found\" });\n    }\n    if (!user.pendingPhone) {\n      return res\n        .status(400)\n        .json({ success: false, message: \"No phone change requested\" });\n    }\n    if (user.resetOTP !== otp || !user.otpExpiry || user.otpExpiry < Date.now()) {\n      return res\n        .status(400)\n        .json({ success: false, message: \"Invalid or expired OTP\" });\n    }\n\n    user.phone = user.pendingPhone;\n    user.pendingPhone = \"\";\n    user.resetOTP = \"\";\n    user.otpExpiry = null;\n    await user.save();\n\n    res.json({\n      success: true,\n      message: \"Mobile number updated successfully\",\n      user: publicUser(user),\n    });\n  } catch (err) {\n    console.log(err);\n    res.status(500).json({ success: false, message: \"Could not update mobile number\" });\n  }\n});\n\napp.post(\"/api/auth/change-password\", requireUser, async (req, res) => {\n  try {\n    const { currentPassword, newPassword } = req.body;\n\n    if (!currentPassword || !newPassword) {\n      return res\n        .status(400)\n        .json({ success: false, message: \"Please enter your current and new password\" });\n    }\n    if (String(newPassword).length < 8) {\n      return res.status(400).json({\n        success: false,\n        message: \"New password must be at least 8 characters\",\n      });\n    }\n\n    const user = await User.findById(req.user.userId).select(\"+passwordHash\");\n    if (!user) {\n      return res.status(404).json({ success: false, message: \"Account not found\" });\n    }\n\n    const match = await bcrypt.compare(String(currentPassword), user.passwordHash);\n    if (!match) {\n      return res\n        .status(400)\n        .json({ success: false, message: \"Current password is incorrect\" });\n    }\n\n    user.passwordHash = await bcrypt.hash(String(newPassword), 10);\n    user.resetOTP = \"\";\n    user.otpExpiry = null;\n    await user.save();\n\n    res.json({ success: true, message: \"Password updated successfully\" });\n  } catch (err) {\n    console.log(err);\n    res.status(500).json({ success: false, message: \"Could not update password\" });\n  }\n});\n\napp.get(\"/api/auth/me\", requireUser, async (req, res) => {\n  const user = await User.findById(req.user.userId);",
});

let next = src;
for (const e of edits) {
  if (!next.includes(e.from)) {
    console.error("MARKER NOT FOUND:\n" + e.from.slice(0, 150));
    process.exit(1);
  }
  next = next.replace(e.from, e.to);
}
fs.writeFileSync(p, next);
console.log("Backend Login & Security endpoints added ✔");
